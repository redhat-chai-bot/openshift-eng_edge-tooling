requests>=2.32.2,<3
pyyaml>=6.0,<7
# Optional: Kerberos auth for Errata Tool and stage Pyxis.
# Requires krb5-devel system headers to build.
# Install separately: pip install 'requests-gssapi>=1.3.0,<2'
